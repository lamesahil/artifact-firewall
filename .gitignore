# Dependencies
node_modules/

# Environment configurations — ignore secrets, allow public build-time vars
.env
.env.local
# .env.production is intentionally tracked (contains only public API URLs, no secrets)

# Build outputs
dist/
build/

# IDE/System files
.system_generated/
.DS_Store
